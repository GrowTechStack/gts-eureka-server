server:
  port: 8761

spring:
  application:
    name: gts-eureka-server

eureka:
  client:
    register-with-eureka: false  # 자기 자신은 등록 안 함
    fetch-registry: false         # 자기 자신은 조회 안 함
  server:
    enable-self-preservation: false  # 개발 환경에서 자기보호 모드 비활성화
